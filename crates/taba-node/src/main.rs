//! Binary entry point for the taba node daemon.

fn main() {
    eprintln!("taba daemon — not yet implemented (M3)");
    eprintln!("Run 'cargo test -p taba-node' for unit tests.");
    std::process::exit(1);
}
