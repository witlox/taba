//! Step definitions for `node_lifecycle.feature`.
//!
//! Most steps are handled by [`common`]. This module contains
//! steps specific to the `node_lifecycle` feature that are not covered
//! by the common step definitions.

// Steps for node_lifecycle.feature are handled by common.rs where possible.
// Feature-specific steps will be added as needed.
